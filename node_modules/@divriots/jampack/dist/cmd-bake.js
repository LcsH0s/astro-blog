"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const packer_1 = require("./packer");
const fast_glob_1 = __importDefault(require("fast-glob"));
const chalk_1 = __importDefault(require("chalk"));
async function bake(config, options) {
    const isProduction = !options.quick;
    if (isProduction)
        process.env.NODE_ENV = "production";
    const entries = await fast_glob_1.default(["**/index.html"], {
        cwd: config.rootDir,
        followSymbolicLinks: false,
    });
    if (entries.length <= 0) {
        throw new Error("No index.html found in " + config.rootDir);
    }
    console.log("Targeting:");
    entries.forEach((f) => console.log(chalk_1.default.gray("> " + f)));
    await packer_1.pack(config.rootDir, entries, isProduction, config);
}
exports.default = bake;
